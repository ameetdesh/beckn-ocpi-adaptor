import { z } from "zod";

export default z.object({ "id": z.string().describe("Client-supplied identifier for this media input.").optional(), "type": z.enum(["image","audio","video"]).describe("Media category."), "url": z.string().url().describe("HTTPS URL or data URI pointing to the media resource."), "contentType": z.string().describe("MIME type, e.g., image/jpeg, audio/mpeg, video/mp4.").optional(), "textHint": z.string().describe("Optional pre-extracted text (OCR/ASR) for search augmentation.").optional(), "language": z.string().describe("Language code (BCP-47) of `textHint` or spoken audio.").optional(), "startMs": z.number().int().describe("Optional start offset in milliseconds (for audio/video segments).").optional(), "endMs": z.number().int().describe("Optional end offset in milliseconds (for audio/video segments).").optional() }).strict().describe("Reference to an image, audio clip, or video used for multimodal search.");
